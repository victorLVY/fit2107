from person import Person

class Attendee(Person):
    def __init__():
        raise NotImplementedError

    # attendee: a person who attends the event

    # attendees will be notified of the event at creation, change and cancellation

    # they can accept and reject invitations 

    # are only able to review events for max of +-5 years.

    # max of 20 attendees for each event.
    
